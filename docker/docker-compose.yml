services:
  neo4j:
    image: neo4j:5-community
    environment:
      NEO4J_AUTH: neo4j/addadev123
      NEO4J_PLUGINS: '["apoc","graph-data-science"]'
      NEO4J_dbms_security_procedures_unrestricted: apoc.*,gds.*
    ports:
      - "${ADDA_NEO4J_HTTP_PORT:-7475}:7474"
      - "${ADDA_NEO4J_BOLT_PORT:-7688}:7687"
    volumes:
      - neo4j_data:/data

  ollama:
    image: ollama/ollama:latest
    ports:
      - "${ADDA_OLLAMA_PORT:-11435}:11434"
    volumes:
      - ollama_data:/root/.ollama

  redis:
    image: redis:7-alpine
    ports:
      - "${ADDA_REDIS_PORT:-6380}:6379"

  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    depends_on:
      - neo4j
      - ollama
      - redis
    environment:
      NEO4J_URI: bolt://neo4j:7687
      NEO4J_USER: neo4j
      NEO4J_PASSWORD: addadev123
      OLLAMA_HOST: http://ollama:11434
      OLLAMA_MODEL: qwen2.5:7b
      ADDA_PULL_OLLAMA: ${ADDA_PULL_OLLAMA:-1}
      REDIS_URL: redis://redis:6379/0
    ports:
      - "${ADDA_API_PORT:-8001}:8000"

volumes:
  neo4j_data:
  ollama_data:
